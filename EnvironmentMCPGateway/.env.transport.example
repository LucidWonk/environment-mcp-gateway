# MCP Transport Configuration Examples
# Copy to .env.development and modify as needed

# =============================================================================
# MCP TRANSPORT CONFIGURATION
# =============================================================================

# Transport Type: 'stdio' for backward compatibility, 'http' for multi-client support
# Default: 'http'
MCP_TRANSPORT_TYPE=http

# HTTP Server Port (when using HTTP transport)
# Default: 3001
MCP_SERVER_PORT=3001

# Enable dual transport mode (both STDIO and HTTP simultaneously)
# Default: false
MCP_ENABLE_DUAL_TRANSPORT=false

# MCP Logging Level
# Options: error, warn, info, debug
# Default: info
MCP_LOG_LEVEL=info

# =============================================================================
# BACKWARD COMPATIBILITY - STDIO MODE
# =============================================================================

# To use legacy STDIO transport (single-client only):
# MCP_TRANSPORT_TYPE=stdio
# MCP_ENABLE_DUAL_TRANSPORT=false

# =============================================================================
# MULTI-CLIENT HTTP MODE (Recommended)
# =============================================================================

# For multi-client HTTP transport with session management:
# MCP_TRANSPORT_TYPE=http
# MCP_SERVER_PORT=3001
# MCP_LOG_LEVEL=info

# =============================================================================
# MIGRATION CONFIGURATION
# =============================================================================

# During migration period, enable dual transport:
# MCP_TRANSPORT_TYPE=http
# MCP_ENABLE_DUAL_TRANSPORT=true
# MCP_SERVER_PORT=3001

# This allows:
# - Existing STDIO clients to continue working
# - New HTTP clients to connect concurrently
# - Gradual migration without disruption

# =============================================================================
# CLIENT CONFIGURATION EXAMPLES
# =============================================================================

# For Claude Code HTTP Client:
# {
#   "mcpServers": {
#     "lucidwonks-environment": {
#       "command": "node",
#       "args": ["dist/server.js"],
#       "transport": "sse",
#       "url": "http://localhost:3001/mcp"
#     }
#   }
# }

# For Claude Code STDIO Client (backward compatibility):
# {
#   "mcpServers": {
#     "lucidwonks-environment": {
#       "command": "node",
#       "args": ["dist/server.js"],
#       "env": {
#         "MCP_TRANSPORT_TYPE": "stdio"
#       }
#     }
#   }
# }