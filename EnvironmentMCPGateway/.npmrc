# Ensure platform-specific binaries are installed correctly
# Prevent cross-platform binary issues in WSL
target_platform=linux
target_arch=x64

# Cache settings for stability
cache-min=3600
prefer-offline=false
save-exact=true

# Prevent Windows/WSL conflicts
registry=https://registry.npmjs.org/