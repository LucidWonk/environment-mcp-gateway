{
  "mcpServers": {
    "lucidwonks-environment-gateway": {
      "command": "docker",
      "args": [
        "exec", 
        "-i", 
        "environment-mcp-gateway",
        "node",
        "/app/dist/server.js"
      ],
      "env": {
        "PROJECT_ROOT": "/workspace"
      }
    }
  }
}